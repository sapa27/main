{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.5.0",
  "private": true,
  "description": "Cloud Run production frontend and API gateway for sapa27 with mobile-safe Meeting loading",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
