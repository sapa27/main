{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.7.0",
  "private": true,
  "description": "CR-7 direct-only Cloud Run frontend and GAS gateway with no GitHub Pages runtime dependency",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
