{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.7.0",
  "private": true,
  "description": "Cloud Run production frontend and server-to-server GAS gateway; CR-7 runtime decoupled from GitHub Pages",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
