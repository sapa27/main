{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.6.0",
  "private": true,
  "description": "Cloud Run production frontend and API gateway for sapa27 with nested API classification, active read caching, and faster deferred asset loading",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
