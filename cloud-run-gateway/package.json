{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.7.2",
  "private": true,
  "description": "CR-7 direct-only Cloud Run cutover using the new GAS direct-JSON deployment",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
