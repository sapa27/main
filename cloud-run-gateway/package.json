{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.8.0",
  "private": true,
  "description": "CR-8 Cloud Run gateway with GAS-owned canonical direct JSON response envelope",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
