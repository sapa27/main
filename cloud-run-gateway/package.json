{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.7.1",
  "private": true,
  "description": "CR-7 direct-only Cloud Run runtime decoupling; production gated by direct GAS canary",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
