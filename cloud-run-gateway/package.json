{
  "name": "sapa27-cloud-run-gateway",
  "version": "0.7.3",
  "private": true,
  "description": "CR-7 direct-only cutover; GAS direct JSON canary passed, smoke false-negative fixed",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js",
    "test": "node --test test/*.test.js"
  }
}
